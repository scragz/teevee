# Teevee

 Teevee is a **Feedback Ring Buffer** acting as a real-time "Slit-Scan" audio processor. It turns a stream of time (Audio) into a static block of memory (Video), manipulate it via GPU shaders, and decodes it back into time.
